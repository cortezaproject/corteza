modules:
  modA:
    fields:
      refC:
        kind: Record
        options:
          module: modC
      refB:
        kind: Record
        options:
          module: modB

  modC: { fields: { fc1: { kind: String } } }
  modB: { fields: { fb1: { kind: String } } }
