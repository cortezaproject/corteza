// eslint-disable-next-line
import { default as component } from './Hint.vue'

const props = {
  text: 'I\'m the hint!',
  id: '34324',
}

export default {
  name: 'Hint',
  group: ['Common'],
  component,
  props,
  controls: [],
}
