deny:
  indexer_private:
    corteza::system/:
      - applications.search
      - auth-clients.search
      - users.search
      - templates.search
      - roles.search

  indexer_protected:
    corteza::system/:
      - applications.search
      - auth-clients.search
      - users.search
      - templates.search
      - roles.search

  indexer_public:
    corteza::system/:
      - applications.search
      - auth-clients.search
      - users.search
      - templates.search
      - roles.search
