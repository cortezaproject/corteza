allow:
  admin:
    corteza::federation/:
      - grant
      - pair
      - settings.read
      - settings.manage
      - node.create
      - nodes.search

    corteza::federation:node/*:
      - manage
      - module.create

    corteza::federation:exposed-module/*:
      - manage

    corteza::federation:shared-module/*:
      - map

  federation:
    corteza::federation/:
      - grant
      - pair
      - settings.read
      - settings.manage
      - node.create
      - nodes.search

    corteza::federation:node/*:
      - manage
      - module.create

    corteza::federation:exposed-module/*/*:
      - manage

    corteza::federation:shared-module/*/*:
      - map
