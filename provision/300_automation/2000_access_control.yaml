allow:
  admins:
    corteza::automation/:
      - grant
      - workflow.create
      - workflows.search
      - triggers.search
      - sessions.search

    corteza::automation:workflow/*:
      - read
      - update
      - delete
      - undelete
      - execute
      - triggers.manage
      - sessions.manage

  developers:
    corteza::automation/:
      - grant
      - workflow.create
      - workflows.search
      - triggers.search
      - sessions.search

    corteza::automation:workflow/*:
      - read
      - update
      - delete
      - undelete
      - execute
      - triggers.manage
      - sessions.manage
