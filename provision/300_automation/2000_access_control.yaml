allow:
  admins:
    corteza::automation/:
      - grant
      - workflow.create
      - triggers.search
      - sessions.search

    corteza::automation:workflow/*:
      - read
      - update
      - delete
      - execute
      - triggers.manage
      - sessions.manage

  developers:
    corteza::automation/:
      - grant
      - workflow.create
      - triggers.search
      - sessions.search

    corteza::automation:workflow/*:
      - read
      - update
      - delete
      - execute
      - triggers.manage
      - sessions.manage


