roles:
  # Placeholders for programmatically created roles.
  # These are created in provision/roles.go
  admins:
    name: Administrators
  everyone:
    name: Everyone

  federation:
    name: Federation
