name: corredor

functions:
  exec:
    kind: function
    meta:
      short: Executes script in Corredor Automation server
    params:
      script: { types: [ { wf: String } ], required: true }
      args:  { types: [ { wf: Any } ] }
    results:
      results:
        wf: Any
