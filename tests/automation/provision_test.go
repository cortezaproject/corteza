package automation

import (
	"testing"
)

func TestProvisioning(t *testing.T) {
	t.SkipNow()
	//h := newHelper(t)
	//
	//readers, err := impAux.ReadStatic(provision.Asset)
	//h.noError(err)
	//h.noError(importer.Import(ctx, nil, readers...))
}
