namespace: ns1
charts:
  chr1:
    name: chr1 name
    config:
      reports:
      - dimensions:
        - field: Status
          modifier: (no grouping / buckets)
        filter: ""
        metrics:
        - backgroundColor: '#11ff57'
          field: count
          fixTooltips: true
          type: pie
        module: mod1
        renderer: {}
      colorScheme: tableau.Tableau10
