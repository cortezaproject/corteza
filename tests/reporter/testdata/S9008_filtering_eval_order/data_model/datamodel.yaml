namespaces:
  ns:
    name: ns name

modules:
  user:
    records:
      source: data_users.csv
      key: id
      mapping:
        id: /
        first_name:
          field: first_name
        numbers:
          field: numbers

    fields:
      first_name:
        label: first_name label
        kind: String
      numbers:
        label: numbers label
        kind: Number
